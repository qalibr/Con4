import React, { useState } from 'react';
import { supabase } from '../supabaseClient'


function Login() {
        return (
            <>
                    Login
            </>
        )
}

export default Login;